<h1>Links</h1>
 
<table class="table">
  <tr>
    <th>Verfügbare Links</th>
  </tr>
 
  <% @links.each do |link| %>
    <tr>
      <td><%= link_to(link.title, link_path(link)) %></td>
    </tr>
  <% end %>
</table>

<%= link_to 'Erstellen', new_link_path, class: "btn btn-primary"%>