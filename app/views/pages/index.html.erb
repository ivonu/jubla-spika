<h1>Seiten</h1>
 
<table class="table">
  <tr>
    <th>Titel</th>
  </tr>
 
  <% @pages.each do |page| %>
    <tr>
      <td><%= link_to(page.title, edit_page_path(page)) %></td>
    </tr>
  <% end %>
</table>